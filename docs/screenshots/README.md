# Screenshots

Drop the three README screenshots here:

- `overview.png` — the Overview tab
- `docker.png` — the Docker tab
- `container-detail.png` — a container detail sheet

Suggested: portrait, real device or emulator, dark mode (the default), ~1080px wide.
