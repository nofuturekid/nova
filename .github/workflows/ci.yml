name: CI

# ci.yml runs only on PRs (the gating signal for branch protection) and on
# manual dispatch (for backfill / rebuild of a specific commit). It does
# NOT run on push:main — the squash-merge would otherwise rebuild the
# exact same source the PR-CI just built minutes earlier under a different
# SHA, wasting ~5 minutes per code PR.
#
# release.yml resolves the artifact via the PR# embedded in the merge
# commit's message; see ADR-0014.
#
# Job structure (ADR-0019): `setup` runs first to detect docs-only changes
# and gate everything else. `build-debug` and `build-release` run in
# parallel — independent compilations sharing the Gradle build cache
# warmed by cache-warm.yml (ADR-0018). `build` is a tiny aggregator that
# branch protection requires as the single status check.
on:
  pull_request:
  workflow_dispatch:

# Least-privilege GITHUB_TOKEN (CodeQL actions/missing-workflow-permissions).
# CI only checks out + builds + uploads artifacts within the same run —
# read access to repo contents is all it needs.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0   # need history so docs-only diff works on PRs and pushes

      # Skip the heavy Android build when only documentation changed. The
      # aggregator `build` job picks this up via the `setup.outputs.code`
      # output, so branch protection's single required check stays SUCCESS
      # without spinning up the parallel compile jobs. Falls back to a full
      # build whenever we can't reliably diff (force-push, new branch,
      # manual dispatch).
      - name: Detect docs-only change
        id: filter
        run: |
          set -e
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Manual dispatch → full build."
            echo "code=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE="${{ github.event.pull_request.base.sha }}"
            HEAD="${{ github.event.pull_request.head.sha }}"
          else
            BASE="${{ github.event.before }}"
            HEAD="${{ github.sha }}"
          fi
          if [ -z "$BASE" ] || [ "$BASE" = "0000000000000000000000000000000000000000" ]; then
            echo "No diff base (force-push / new branch) → full build."
            echo "code=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CHANGED=$(git diff --name-only "$BASE" "$HEAD")
          echo "Changed files:"
          echo "$CHANGED" | sed 's/^/  /'
          # Whitelist: anything matching this regex is treated as non-shipping
          # (docs + AI assistant context). Everything else (code, gradle,
          # workflows, manifest, resources, …) forces a full build.
          #
          #   *.md                       — README, CLAUDE.md, AGENTS.md, GEMINI.md, …
          #   LICENSE                    — legal text
          #   docs/                      — long-form docs (future-proof, dir doesn't exist yet)
          #   .gitattributes, .editorconfig — repo hygiene
          #   .claude/, .cursor/, .continue/, .aider*, .windsurfrules, .cursorrules, …
          #                              — AI assistant config (Claude Code, Cursor, Continue, Aider, Windsurf)
          #   .github/instructions/      — Copilot path-specific instructions
          NON_DOCS=$(echo "$CHANGED" | grep -vE '\.md$|^LICENSE$|^docs/|^\.gitattributes$|^\.editorconfig$|^\.(claude|cursor|continue|aider|windsurf|sourcegraph|codeium)|^\.github/instructions/' || true)
          if [ -z "$NON_DOCS" ]; then
            echo "::notice::Docs-only change — skipping APK build."
            echo "code=false" >> "$GITHUB_OUTPUT"
          else
            echo "Non-docs files changed → full build."
            echo "code=true" >> "$GITHUB_OUTPUT"
          fi

  build-debug:
    needs: setup
    if: needs.setup.outputs.code == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle (build cache)
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          log-accepted-android-sdk-licenses: false

      - name: Assemble debug + lint
        run: ./gradlew :app:assembleDebug :app:lintDebug --stacktrace

      - name: Upload debug APK
        uses: actions/upload-artifact@v6
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 30
          if-no-files-found: error

      - name: Upload lint report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: lint-report
          path: app/build/reports/lint-results-debug.html
          retention-days: 14
          if-no-files-found: warn

  build-release:
    needs: setup
    if: needs.setup.outputs.code == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle (build cache)
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: ${{ github.event_name == 'pull_request' }}

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          log-accepted-android-sdk-licenses: false

      - name: Decode release keystore (if available)
        id: keystore
        env:
          KEYSTORE_B64: ${{ secrets.KEYSTORE_B64 }}
        run: |
          if [ -z "$KEYSTORE_B64" ]; then
            echo "::warning::KEYSTORE_B64 not available (fork PR?); skipping release build"
            echo "available=false" >> "$GITHUB_OUTPUT"
          else
            echo "$KEYSTORE_B64" | base64 -d > app/release.keystore
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Assemble release
        if: steps.keystore.outputs.available == 'true'
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: ./gradlew :app:assembleRelease --stacktrace

      - name: Verify release APK signature
        if: steps.keystore.outputs.available == 'true'
        run: |
          BT="$ANDROID_HOME/build-tools"
          LATEST=$(ls "$BT" | sort -V | tail -1)
          APK=$(ls app/build/outputs/apk/release/*.apk | head -1)
          "$BT/$LATEST/apksigner" verify --verbose "$APK"

      - name: Upload release APK
        if: steps.keystore.outputs.available == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: app-release
          path: app/build/outputs/apk/release/*.apk
          retention-days: 90
          if-no-files-found: error

      - name: Upload mapping
        if: steps.keystore.outputs.available == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: mapping
          path: app/build/outputs/mapping/release/mapping.txt
          retention-days: 90
          if-no-files-found: warn

  # Aggregator job — branch protection's single required status check.
  # Passes if setup said docs-only (no compile needed) OR both build jobs
  # succeeded. Lets us add/remove parallel jobs in the future without
  # having to update branch-protection config.
  build:
    needs: [setup, build-debug, build-release]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate result
        env:
          DOCS_ONLY: ${{ needs.setup.outputs.code != 'true' }}
          DEBUG_RESULT: ${{ needs.build-debug.result }}
          RELEASE_RESULT: ${{ needs.build-release.result }}
        run: |
          if [ "$DOCS_ONLY" = "true" ]; then
            echo "::notice::Docs-only change — APK build skipped. No version bump required."
            exit 0
          fi
          if [ "$DEBUG_RESULT" = "success" ] && [ "$RELEASE_RESULT" = "success" ]; then
            echo "Both build jobs succeeded."
            exit 0
          fi
          echo "::error::Build failure — debug=$DEBUG_RESULT release=$RELEASE_RESULT"
          exit 1
